module "avm-res-network-azurefirewall" {
  source                        = "../"
  location                      = var.location
  name                          = var.name
  resource_group_name           = var.resource_group_name
  firewall_policy_name          = var.firewall_policy_name
  policy_resource_group_name    = var.policy_resource_group_name
  public_ip                     = var.public_ip
  public_ip_management          = var.public_ip_management
  virtual_network               = var.virtual_network
  virtual_network_management    = var.virtual_network_management
  virtual_hub                   = var.virtual_hub
  virtual_network_id            = var.virtual_network_id
  virtual_network_id_management = var.virtual_network_id_management
  virtual_hub_id                = var.virtual_hub_id
  firewall_sku_name             = var.firewall_sku_name
  firewall_sku_tier             = var.firewall_sku_tier
  diagnostic_settings           = var.diagnostic_settings
  enable_telemetry              = var.enable_telemetry
  firewall_private_ip_ranges    = var.firewall_private_ip_ranges
  firewall_timeouts             = var.firewall_timeouts
  firewall_zones                = var.firewall_zones
  lock                          = var.lock
  role_assignments              = var.role_assignments
  tags                          = var.tags
  app_id         = var.app_id
  msftmigration  = var.msftmigration
  environment    = var.environment
}